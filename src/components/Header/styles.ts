import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        fontSize: 36,
        fontWeight: 'bold',
        color: '#fff',
        textAlign: 'center',
        marginTop: 30,
        
    },

    container: {
        marginTop: 40,        
        alignItems: 'center',                
        height: 200,
        borderColor: "white",        
        borderBottomWidth: 1,
        textAlign: 'center',
    }
});